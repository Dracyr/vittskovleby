.row
  .col-md-6.col-md-offset-3
    .heading
      %h2
        = t('.images')
        = link_to i(:plus), new_image_path, class: 'pull-right' if can? :create, Image
      %hr/
.row
  = render @images
