- if can? :create, Event
  - content_for :navbar_tools do
    = link_to t('.create_event'), new_event_path, class: "btn btn-xs btn-primary"

  - breadcrumb :events

- @events.each do |event|
  .panel.panel-default
    .panel-heading
      %h3
        = event.title
    .panel-body
      = raw event.content
      .pull-right
        = link_to t('.read_more'), event
