require 'spec_helper'

describe Reservation do
  context "is invalid" do
    it "without name"
    it "without email"
    it "without phone"
    it "without date"
    it "without correct email format"
    it "without locations"
    it "without unique locations"
  end
end
